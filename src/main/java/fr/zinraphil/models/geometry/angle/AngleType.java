package fr.zinraphil.models.geometry.angle;

/**
 * This class defines an enum type that represents the different types of angles.
 */
public enum AngleType {
    /* An angle measured in degrees. */
    DEGREE,
    /* An angle measured in radians. */
    RADIAN
}
